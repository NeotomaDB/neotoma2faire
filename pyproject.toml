[project]
name = "neotoma2faire"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Simon", email = "simon.j.goring@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
    "psycopg[binary]>=3.3.2",
    "python-dotenv>=1.2.1",
]

[project.scripts]
neotoma2faire = "neotoma2faire:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
